# This image only supplies an amd64 Linux userspace to execute OpenWrt's
# x86_64-hosted SDK compiler.  The SDK, source, and output are mounted at run
# time; the build container is networkless and receives no host credentials.
FROM debian@sha256:88200866dfff7ea7f5cbcb6ec7c8a701889efe6fe859fe64d6990e4b07ea4171

WORKDIR /work
