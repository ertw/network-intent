[package]
name = "intent-witness-agent"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
intent-protocol.workspace = true
intent-identity = { path = "../identity" }
serde_json = { version = "1.0", features = ["preserve_order"] }
serde.workspace = true
tokio = { version = "1.49", features = ["net", "time", "rt", "macros", "io-util"] }
socket2 = "0.6"
reqwest = { version = "0.12.28", default-features = true }
hickory-proto = "0.25.2"
rand = "0.9"
thiserror.workspace = true
rusqlite = { version = "0.32", features = ["bundled"] }

[features]
native-ubus = []

[build-dependencies]
cc = "1.2"

[dev-dependencies]
tempfile.workspace = true

[[bin]]
name = "intent-witness-agent"
path = "src/main.rs"

[[bin]]
name = "intent-ubus-observe"
path = "src/bin/intent-ubus-observe.rs"
required-features = ["native-ubus"]
