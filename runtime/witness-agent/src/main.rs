//! Witness execution is normally invoked by the agent service.  This binary is
//! intentionally inert until that authenticated assignment transport exists.
fn main() {
    eprintln!("intent-witness-agent: assignment transport is not configured");
}
