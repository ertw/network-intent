# Current checkpoint

Updated 2026-09-17. The full Network Intent suite remains incomplete.

## Baseline

- `71387b0`: compiler/runtime foundations; recorded compiler checks and eight
  native/browser parity fixtures passed.
- `d33310c`: confirmation, witness queue, guarded journal and observation boundary;
  recorded Rust workspace result: 165 tests passed.
- `788b486`: pinned OpenWrt lab and permission evidence for both releases.
- `d527622`: accepted G01/G02/G09 fixture index, Svelte harness and developer guide.
- `2774655`: accepted G03/G04/G05 view controls, snapshot and assurance panels;
  43 unit tests, 26 Chromium tests, type checks and build passed in review.
  These commits are included in local `main`.

The authoritative current work instructions are [grok/CURRENT.md](grok/CURRENT.md).
G06/G07/G08 remain unimplemented. Grok works only on the manually launched batch;
Astra reviews actual diffs before acceptance. The automatic continuation heartbeat
is paused. Do not resume it or create a replacement implicitly.

## Material limits

Current router examples remain admission-blocked by incomplete semantic and
operational coverage. The UI is a labelled component harness, not the integrated
product. Native C/Rust adapter compilation/execution, real apply services, complete
assurance generation, semantic editing/history and product integration remain open.
Physical hardware acceptance is incomplete; no dedicated lab device is available
and no hardware profile is enabled. Native build scaffolding remains experimental.

Compiler semantics, authoritative projection/comparison/health, PKI/Cedar, signed
evidence, durable state, services, timed deployment/recovery, telemetry, packaging
and hardware acceptance remain Astra work. The [full technical scope](implementation-plan.md)
is unchanged. No goal completion is claimed.

## Evidence lookup — read only when needed

[Review gates](grok/REVIEW-GATES.md), [developer guide](developer-guide.md),
[OpenWrt lab](openwrt-lab.md), and [runtime contracts](runtime-contracts.md).
Historical reports, raw logs and progress snapshots are evidence, not instructions
for resuming old processes or batches. See [documentation map](README.md).
