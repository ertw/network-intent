# Task G09 — implementation report

- Base HEAD: `788b486863db9af36e3e932bfed899396c708ac6`
- Task and packet: [G09-documentation.md](../tasks/G09-documentation.md)
- Outcome: Implemented awaiting Astra review
- Owned files changed:
  - `README.md` (modified)
  - `docs/developer-guide.md` (new)
  - `docs/grok/reports/G09.md` (new)
- Other changed files observed (do not claim authorship): G01 indexer files and
  `frontend/` (G02). Coordinator later linked `frontend/README.md` after G02
  landed; G09 still does not claim G02 npm results.

## Behavior delivered

Documentation only. No code, contracts, evidence logs, or goal-progress files were edited.

`README.md` keeps the compiler build/use instructions, `netc` examples, language
3.0 / compiler 0.3.0 notes, and existing language/capability text. The former
blanket line that live observation and deployment were outside “this release”
now names the **compiler release** explicitly and points at in-tree runtime/lab
work so readers do not treat the whole repository as excluding that work. A
**Current status** section links the full objective, recorded progress,
developer guide, OpenWrt lab, and bounded Grok handoff, and states that current
router examples remain admission-blocked and hardware acceptance is incomplete.
The README does not claim services or deployment are ready.

`docs/developer-guide.md` records:

- Local toolchains as already documented: Idris 2 **0.8.0**, Rust minimum
  **1.89**, Make, Python 3.10+, Node only as invoked by the browser-parity
  script. No OS support or install recipes.
- Executable Make targets from the current Makefile only: `make build`,
  `make test`, `make test-browser`, `make test-runtime` / `cargo test --workspace`,
  plus `make clean`. Frontend npm scripts and native/lab/hardware jobs are
  labeled pending or lab-only, not Makefile targets.
- Six verification layers, each with recorded vs pending status and evidence
  files: source/compiler checking; runtime host tests; captured VM fixtures;
  stock-ubus permission tests; native adapter execution; physical acceptance.
- `frontend/` as a fixture component harness awaiting Astra integration.
  Coordinator linked [frontend/README.md](../../../frontend/README.md) after
  G02 landed. G09 does not claim G02 npm results.
- Contribution flow: bounded Grok batch, uncommitted diff plus actual check
  results, user-triggered Astra review. Do not resume
  `resume-network-intent-assurance`.

## Verification

| Command (include working directory) | Exit code | What it establishes |
|---|---|---|
| Python markdown-link resolver over `README.md` and `docs/developer-guide.md` (repo root) | 0 | Every new and preserved repository-relative Markdown link in those files resolves on the filesystem (0 missing). |
| Same resolver over this report after write (repo root) | 0 | Report Markdown links resolve. |
| `git diff --check` (repo root) | 0 | No whitespace errors in the G09 diff. |
| `rg` word search on edited docs for `complete`, `safe`, `verified`, `exactly once`, `production ready` (repo root) | 0 | Hits listed below; no `safe`, `exactly once`, or `production ready`. |
| Compare `Makefile` phony targets to commands named as executable in the guide | n/a (file read) | Makefile targets are only `build`, `test`, `test-runtime`, `test-browser`, `clean`. |
| `test -e frontend/README.md` (repo root, worker write) | 1 | Harness README absent at G09 write time. |
| `test -e frontend/README.md` (repo root, coordinator reconcile) | 0 | README present; guide and root README now link it. G09 still does not claim npm results. |

Skipped / not run (packet: documentation only; do not obtain new experimental claims):

- `make build`, `make test`, `make test-runtime`, `make test-browser`
- `cargo test --workspace` / `cargo test -p intent-apply-agent`
- QEMU/OpenWrt lab, native adapter build, physical device checks
- Any `frontend/` npm scripts

No screenshots. No credentials or device data.

### Word-search results (edited docs)

| Location | Term | Justification |
|---|---|---|
| `README.md` “Native macOS builds/tests are verified” | verified | Pre-existing README sentence, preserved. Qualified as native macOS; Nix still unverified on the development host. |
| `README.md` “not an idempotent deployment delta or complete device image” | complete | Pre-existing negation. |
| `docs/developer-guide.md` “they are not complete services” | complete | Qualified negative, citing `docs/goal-progress.md`. |

No new unqualified “verified” / “complete” / “safe” / “exactly once” /
“production ready” claims were added.

### Claim → supporting files

New documentation claims rest on existing files. Historical counts were not
changed.

| Claim | Supporting files |
|---|---|
| Language 3.0, compiler 0.3.0 | `network-intent.ipkg`, `README.md`, `docs/schema/3.0.json` |
| Idris 2 0.8.0; Python 3.10+; native macOS compiler checks recorded | `README.md`, `docs/verification.md` |
| Rust minimum 1.89 | `Cargo.toml` `workspace.package.rust-version` |
| Make targets `build` / `test` / `test-runtime` / `test-browser` / `clean` | `Makefile` |
| `make test` coverage (compiler) | `docs/verification.md`, `docs/verification-runs/compiler-full-2026-09-15.log` |
| Eight browser/native parity fixtures | `docs/browser-compiler.md`, `scripts/check_browser_parity.py`, `docs/verification-runs/browser-parity-2026-09-15.log`, `docs/goal-progress.md` |
| Runtime foundation `d33310c`; 165 Rust tests passed | `docs/goal-progress.md`, `GROK_HANDOFF.md`, `docs/verification-runs/runtime-integration-2026-09-16.log` |
| Current router examples remain admission-blocked | `docs/goal-progress.md`, `GROK_HANDOFF.md` |
| OpenWrt 25.12.5 and 24.10.8 verified images, baseline fixtures | `docs/openwrt-lab.md`, `docs/verification-runs/openwrt-25.12.5-baseline.log`, `docs/verification-runs/openwrt-24.10.8-baseline.log`, `lab/openwrt/fixtures/` |
| Stock-ubus permission tests passed; not native adapter execution | `docs/openwrt-lab.md`, `docs/verification-runs/openwrt-25.12.5-permissions.log`, `docs/verification-runs/openwrt-24.10.8-permissions.log`, `docs/verification-runs/handoff-review-2026-09-16.md` |
| Host native observation helper tests passed; Linux native-feature compilation and live execution unverified | `runtime/witness-agent/NATIVE-UBUS.md`, `docs/goal-progress.md`, 2026-09-16 integration log |
| Native build experiment incomplete; no runnable native artifact | `lab/openwrt/build-native/README.md`, `docs/goal-progress.md` |
| No physical lab device; hardware acceptance incomplete | `GROK_HANDOFF.md`, `docs/goal-progress.md`, `docs/openwrt-lab.md` |
| Identity/Cedar/mTLS foundations, not complete services | `docs/goal-progress.md` |
| Apply-agent planning + journal; no native UCI transport or production write path | `runtime/apply-agent/README.md`, `docs/goal-progress.md` |
| `resume-network-intent-assurance` paused; bounded Grok batch | `docs/goal-progress.md`, `GROK_HANDOFF.md` |
| Frontend harness awaiting Astra integration; README linked after G02 | `GROK_HANDOFF.md`, `frontend/README.md` (commands only; G09 does not claim those tests), filesystem |

## Limitations / Astra decisions

### Apply-agent test-count contradiction (do not “fix” in forbidden files)

`runtime/apply-agent/README.md` still says:

> Validation: `cargo test -p intent-apply-agent` currently passes four planner
> tests covering binding, drift, unowned preservation, scalar/list behavior,
> deletion, secret rejection, duplicate identity rejection, and no-op plans.

`docs/verification-runs/runtime-integration-2026-09-16.log` shows
`intent_apply_agent` lib tests: **30 passed** (0 failed), including
`journal::tests::*` and six `tests::*` planner cases
(`blocks_unowned_requests_and_section_kind_drift`,
`blocks_unowned_section_create_type_and_reorder_but_preserves_current_drift`,
`drift_and_binding_rejected`, `deletion_noop_and_secrets`,
`duplicate_identity_rejected`, `preserves_unowned_and_scalar_list_order`).
The binary target in that log ran 0 tests.

G09 did not edit `runtime/apply-agent/README.md`. The developer guide cites the
workspace log for host tests and does not repeat the stale “four planner tests”
count. Proposed Astra decision: update the apply-agent README test count in a
later authorized edit, without treating that README as current evidence.

### Frontend README timing

G02 landed `frontend/README.md` in this batch. Coordinator linked it from
`README.md` and `docs/developer-guide.md`. G09 still does not claim G02 tests
passed; those results belong in the G02 and turn reports.

### No new experimental evidence

G09 did not re-run Idris/Rust/browser/VM jobs. Status text restates recorded
files only.

## Scope check

- Owned files only: `README.md`, `docs/developer-guide.md`,
  `docs/grok/reports/G09.md`.
- Did not edit goal-progress, implementation-plan, review gates, other
  handoffs, runtime contracts, existing evidence, compiler/runtime source, or
  G01/G02 files.
- Did not invent Make targets, OS install recipes, roadmap items, architecture,
  or screenshots.
- Did not commit, stage, push, create a PR, write `docs/grok/reports/turn-001.md`,
  or resume `resume-network-intent-assurance`.
- Stay on `codex/grok-easy-tasks` at base `788b486863db9af36e3e932bfed899396c708ac6`.

Stop here for manual review.
